import React from "react";
import ReactDOM from "react-dom";

const App = (
  <div className="app">
    <p className="title">{`Hello World! I am SPA`}</p>
  </div>
);

ReactDOM.render(App, document.getElementById("root"));
